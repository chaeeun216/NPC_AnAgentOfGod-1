using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CutsceneManager : MonoBehaviour
{
    public static bool isFinished = false; // 컷씬이 완전히 불러와진 후 텍스트 출력
    SplashManager theSplashManager; // 컷씬을 불러오려면 splash가 되어야함
    // CameraController theCam; // 카메라 리셋

    // [SerializeField] Image; // 이미지 가져오기

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
